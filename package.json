{
  "repository": {},
  "license": "APACHE-2.0",
  "scripts": {
    "start:firefox": "web-ext run --source-dir ./extension-dist/",
    "build": "web-ext build"
  },
  "devDependencies": {
    "web-ext": "^3.0.0"
  }
}
